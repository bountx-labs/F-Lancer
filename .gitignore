# Environment
.env

# Build artifacts
freelance-engine.exe
/freelance-engine

# Temp/cache (local only, never commit)
.tmp/
.cache/
.plans/

# Local dev state snapshot (if created manually)
# NOTE: state/seen_jobs.json IS committed on purpose - CI needs it for cross-run dedupe.

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db